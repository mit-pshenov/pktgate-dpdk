// src/main.cpp
//
// M0: the binary does nothing. Its only job right now is to prove that
// the project links against DPDK via pkg-config and that the five
// sanitizer presets build end-to-end. Real entry point logic arrives in
// M3 (EAL init + worker skeleton).

int main() {
  return 0;
}
